name = "tracker-optimizer"
main = "tracker-optimizer.js"
compatibility_date = "2024-01-01"

[env.production]
name = "tracker-optimizer"

# Optional: Add custom domain
# routes = [
#   { pattern = "tracker-optimizer.your-domain.com/*", zone_name = "your-domain.com" }
# ]

# Optional: Add environment variables
# [vars]
# ENVIRONMENT = "production"

# Optional: Add secrets (set via wrangler secret put)
# [secrets]
# API_KEY